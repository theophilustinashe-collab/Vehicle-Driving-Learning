services:
  # Backend API Server
  - type: web
    name: vehicle-driving-learning-api-3
    runtime: docker
    dockerFilePath: ./Dockerfile
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false # Set this in the Render dashboard manually
      - key: SESSION_SECRET
        generateValue: true
    healthCheckPath: /api/healthz

  # Frontend Web App
  - type: web
    name: vehicle-driving-learning-3
    runtime: node
    plan: free
    buildCommand: pnpm install --no-frozen-lockfile && pnpm --filter @workspace/vid-master... build
    startCommand: pnpm --filter @workspace/vid-master run serve
    envVars:
      - key: VITE_API_URL
        value: https://vehicle-driving-learning-api-3.onrender.com
      - key: NODE_VERSION
        value: 20.x
      - key: PORT
        value: 3001
    healthCheckPath: /
